# Security check — 2026-07-01

Проект: **FIgma tests** (репо TTD) — статический React+Vite+TS SPA.
Повод: деплой после фикса D57 (кнопки-дропдауны шапки) + D56 (скролл). Изменения — UI/интеракция
(переиспользован существующий компонент `Dropdown`, статические опции; новых зависимостей нет).
Критерий: 🔴 = 0 до push/deploy (CONTRIBUTING.md).

| Проверка | Статус | Результат |
|---|---|---|
| Секреты в `src` | ✅ | `git grep` figd_/ghp_/sk_live/AKIA/api_key/secret/password — пусто |
| `.env` в git | ✅ | не отслеживается (`.gitignore`: `.env*`, `.vercel`) |
| XSS / инъекции рендера | ✅ | нет `dangerouslySetInnerHTML`/`innerHTML`/`eval`/`new Function`/`document.write` |
| Пользовательский ввод | ✅ | статические данные/пропсы; dropdown-опции — захардкоженные демо-значения |
| Зависимости (`npm audit`) | ⚠️ | 2 (1 high, 1 mod) в `vite`/`esbuild` — **dev-tooling**, на прод-статику не влияет → `security-todo` (фикс = vite@8, breaking) |
| Прод-артефакт | ✅ | `npm run build` → статический `dist/`; runtime-зависимостей с уязвимостями нет |

## Изменения с прошлого отчёта (2026-06-25)
- **D56** — `ScrollRestoration` (скролл наверх при навигации).
- **D57** — CardTop: теги-дропдауны раскрываются (компонент `Dropdown`), пилюли-действия кликабельны.
Новых зависимостей/сетевых вызовов/обработчиков внешнего ввода/опасных паттернов нет → поверхность неизменна.

## Изменения к деплою D58–D68 (перепроверка перед прод-деплоем)
- **D58–D66** — FlowCanvas (node-editor), фон-колонки, прозрачность нод.
- **D67** — границы нод (кламп), зазор панель↔канва 8px.
- **D68** — задвиг нод за панели (кламп/z-index/overflow-x:clip), сворачиваемая панель свойств (`useState`-toggle, inline SVG-шевроны).
- Всё — UI-геометрия/CSS/локальный стейт. Новых зависимостей/сетевых вызовов/обработки внешнего ввода/
  `dangerouslySetInnerHTML`/`eval` нет. `git grep` секретов — пусто; `.env` не трекается; `dist/` без утечек;
  `npm audit --omit=dev` = **0 уязвимостей** (⚠️ vite/esbuild — только dev-tooling, на прод-статику не влияют).

## Изменения к деплою D69–D71 (перепроверка перед прод-деплоем)
- **D69** — CLAUDE_default.md (local-only, в .gitignore, в репо не коммитится).
- **D70** — навигация по табам шапки (`usePageNav`, react-router `navigate`) + `flex-wrap` в TopMenu.
- **D71** — CardTop: теги-дропдауны раскрываются вниз (снят `overflow:hidden`, CSS).
- **D72** — global.css: сброс UA-бордера нативных контролов (чистый CSS).
- Всё — клиентский роутинг/CSS/локальный стейт. Новых зависимостей/сетевых вызовов/обработки внешнего
  ввода/`dangerouslySetInnerHTML`/`eval` нет. `git grep` секретов в новых файлах — пусто; `.env` не
  трекается; `npm audit --omit=dev` = **0 уязвимостей**.

## Финализация проекта (пуш + прод-деплой, 2026-07-01)
Пуш всех локальных коммитов (D70–D72) в TTD + прод-деплой на Vercel. Поверхность неизменна (SPA-статик,
без секретов/сети/внешнего ввода). **🔴 = 0 — push/deploy разрешён.**

## Итог
**🔴 = 0 — push/deploy разрешён.** ⚠️ vite/esbuild (dev-only) — в `audit/security-todo.md`.
