node_modules
dist
dist-ssr
*.local
*.tsbuildinfo
.DS_Store

# Скриншоты этого проекта (референсы Figma + verification) — локальные, не в репозиторий
.screenshots

# Живой снимок Figma, который figma-sync выгружает через MCP перед диффом
.figma-live.json

*.log
.vercel
.env*

# Локальный дефолтный контекст агента (не коммитим — см. DECISIONS D69)
CLAUDE_default.md
