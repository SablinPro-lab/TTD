import { TextArea } from './TextArea'

# TextArea

**Назначение.** Многострочное поле ввода с uppercase-заголовком (как `Input`, но textarea).
Источник правды: Figma → ds-atoms → `text_area` (node `1:1794`). **Статус:** stable.

| prop          | значения  | по умолч. |
| ------------- | --------- | --------- |
| `label`       | `string`  | —         |
| `placeholder` | `string`  | —         |
| `rows`        | `number`  | `3`       |

…плюс нативные атрибуты `<textarea>`.

## Живой вариант

<TextArea label="Head line" placeholder="Type something here" />
