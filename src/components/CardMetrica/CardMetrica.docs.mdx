import { CardMetrica } from './CardMetrica'

# CardMetrica

**Назначение.** Карточка: заголовок + крупное числовое значение.
Источник правды: Figma → ds-molecules → `Cards metrica` (node `1:1942`). **Статус:** stable.

| prop    | значения    | по умолч. |
| ------- | ----------- | --------- |
| `title` | `string`    | —         |
| `value` | `ReactNode` | —         |

## Живой вариант

<CardMetrica title="Applications" value={14} />
