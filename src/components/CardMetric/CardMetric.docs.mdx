import { CardMetric } from './CardMetric'

# CardMetric

**Назначение.** Компактная карточка: заголовок + подпись-итог внизу.
Источник правды: Figma → ds-molecules → `card metric` (node `1:1936`). **Статус:** stable.

| prop      | значения                                                  | по умолч.    |
| --------- | --------------------------------------------------------- | ------------ |
| `title`   | `string`                                                  | —            |
| `caption` | `string`                                                  | —            |
| `values`  | `number[]` (высоты столбцов 0–100)                         | `[100, 55]`  |
| `color`   | `green \| red \| pink \| lavender \| olive \| peach`       | `green`      |

## Живой вариант

<CardMetric title="Health" caption="Overall: good" />

<!-- D48: вместо столбиков — горизонтальный капсульный индикатор (progress-pill): проп progress (0–100), трек белый (control-on-color) + заполнение чёрное (text-primary). Прежний values убран. -->

<!-- D49: возврат к Figma 1:1936 точь-в-точь — вместо капсулы используется компонент Graph (граф из 2 баров, перекрашены в bg-base). Капсула убрана. Проп values. -->

<!-- D50: карточка 205×195; индикатор — широкие чанковые плитки graph (две: высокая+ниже, [100,59]). Исправлен h-full на странице, ломавший высоту графика (бары схлопывались в min-height). По Figma 1:4015-4018 это graph, НЕ капсула. -->

