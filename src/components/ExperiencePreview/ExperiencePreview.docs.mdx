import { ExperiencePreview } from './ExperiencePreview'

# ExperiencePreview

**Назначение.** Блок опыта: период + должность + компания + описание.
Источник правды: Figma → ds-molecules → `experience_preview` (node `1:1912`). **Статус:** stable.

| prop          | значения  | по умолч. |
| ------------- | --------- | --------- |
| `period`      | `string`  | —         |
| `role`        | `string`  | —         |
| `company`     | `string`  | —         |
| `description` | `string`  | —         |

## Живой вариант

<ExperiencePreview
  period="Jan 2022 — present (3 years)"
  role="Senior Frontend Developer"
  company="TechCorp Solutions"
  description="Led development of React applications, mentored junior developers, improved performance by 40%"
/>
